# Core dependencies for edge device (Jetson Orin Nano)
# WICHTIG: Installiere zuerst die Jetson-spezifischen Pakete manuell!

# Basis-Pakete (kompatibel mit Jetson ARM64)
opencv-python  # Verwende System-Version oder pip
pyyaml
requests
pyserial
pynmea2

# YOLOv8 - für Inferenz benötigt
# ultralytics  # Optional für Training/Export, nicht für Inferenz nötig

# ONNX Runtime für Jetson - MANUELL installieren:
# Siehe: https://elinux.org/Jetson_Zoo#ONNX_Runtime
# Oder verwende TensorRT direkt (bereits in JetPack enthalten)

# TensorRT und PyCUDA (bereits in JetPack vorinstalliert)
# tensorrt  # Bereits vorhanden
# pycuda   # Bereits vorhanden

# Async support (optional für Cloud-Kommunikation)
# aiohttp
# asyncio-mqtt
